export enum Lang {
  EN = 'en',
  RU = 'ru',
}

export enum Currency {
  USD = 'USD',
  UAH = 'UAH',
  RUB = 'RUB',
}

export enum Period {
  Range = 'range',
  All = 'all',
  Day = 'day',
  Week = 'week',
  Today = 'today',
  Year = 'year',
  Month = 'month',
}

export enum TransactionType {
  Income = 'income',
  Expenses = 'expenses',
}
